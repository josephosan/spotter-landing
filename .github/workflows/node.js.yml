name: Node.js CI and Server Update

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build-and-deploy:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [20.x]

    steps:

    # SSH into the server and run update commands
    - name: SSH into Server and Update
      uses: appleboy/ssh-action@master
      with:
        host: 109.120.176.63
        username: joseph
        key: ${{ secrets.SSH_PRIVATE_KEY }}
        port: 22
        script: |
          cd /home/joseph/projects/spotter-landing
          git pull origin
          npm run build
          sudo systemctl restart spotter-landing 
